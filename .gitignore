# ===== 官网门户（portal）=====
node_modules/
.nuxt/
.output/
dist/
.env
.env.local
.env.*.local
coverage/
*.log

# ===== 百度站长验证源文件（上传附件，不入版本库；线上用 public/ 那份） =====
docs/baidu_verify_codeva-XN3yApWheb.html

# ===== nginx 配置不纳入 git 控制 =====
# 服务器 nginx 由 monorepo（nginx-host.conf）与本部署脚本（内联分流块）共同维护，
# 配置文件不版本化，避免双源冲突；本地产物保留在工作区作参考
nginx.conf.template
deploy/portal.nginx.split.conf

# ===== 计划与临时文档（本地协作产物，不入版本库） =====
.zcode/
